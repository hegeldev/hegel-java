# Filtered by Maven at build time from the <libhegel.version> pom property.
# The engine release these bindings were built and tested against; used to warn if a
# user-supplied $HEGEL_LIBHEGEL_PATH points at a different version.
engine.version=${libhegel.version}
